diff --git a/agent/aggregator.go b/agent/aggregator.go
index abc123..def456 100644
--- a/agent/aggregator.go
+++ b/agent/aggregator.go
@@ -20,6 +20,7 @@ type Aggregator struct {
 	analyzer    *remediation.Analyzer
 	remediator  *remediation.HybridRemediator
+	autoBlocker *remediation.AutoBlocker  // NEW: Auto-blocking
 	
 	events      []TrafficEvent
 	eventsMu    sync.Mutex
@@ -37,6 +38,22 @@ func NewAggregator(apiKey, serverHost, grpcURL string, remediator *remediation.H
 		analyzer:   analyzer,
 		remediator: remediator,
 	}
+	
+	// NEW: Initialize auto-blocker if remediation enabled
+	if remediator != nil {
+		ipsetRemediator := remediator.GetIPSetRemediator() // Add this method
+		scorer := scoring.NewThreatScorer() // Import scoring package
+		
+		config := remediation.DefaultAutoBlockerConfig()
+		config.Enabled = os.Getenv("KERNELEYE_AUTO_BLOCK") == "true"
+		
+		autoBlocker, err := remediation.NewAutoBlocker(config, scorer, ipsetRemediator)
+		if err != nil {
+			return nil, fmt.Errorf("failed to create auto-blocker: %w", err)
+		}
+		agg.autoBlocker = autoBlocker
+	}
+	
 	return agg, nil
 }
 
@@ -89,6 +106,13 @@ func (agg *Aggregator) ProcessEvent(e Event) {
 			Duration: decision.Duration,
 		})
 	}
+	
+	// NEW: Auto-blocking based on threat score
+	if agg.autoBlocker != nil {
+		// Calculate threat score from event
+		score := agg.calculateThreatScore(e)
+		agg.autoBlocker.ProcessScore(ipStr, score)
+	}
 }
 
 func (agg *Aggregator) calculateThreatScore(e Event) scoring.ThreatScore {

diff --git a/agent/remediation/hybrid_remediator.go b/agent/remediation/hybrid_remediator.go
index abc123..def456 100644
--- a/agent/remediation/hybrid_remediator.go
+++ b/agent/remediation/hybrid_remediator.go
@@ -14,6 +14,11 @@ type HybridRemediator struct {
 	// ... existing fields
 }
 
+// GetIPSetRemediator returns the underlying IPSet remediator
+func (h *HybridRemediator) GetIPSetRemediator() *IPSetRemediator {
+	return h.iptables
+}
+
 // ... rest of methods
 
 diff --git a/agent/config.go b/agent/config.go
index abc123..def456 100644
--- a/agent/config.go
+++ b/agent/config.go
@@ -12,6 +12,8 @@ type Config struct {
 	EnableRemediation bool
 	EnableXDP         bool
 	InterfaceName     string
+	// NEW: Auto-blocking config
+	AutoBlockConfig   remediation.AutoBlockerConfig
 }
 
 func parseConfig() Config {
@@ -25,6 +27,14 @@ func parseConfig() Config {
 	cfg.EnableXDP = os.Getenv("KERNELEYE_XDP") == "true"
 	cfg.InterfaceName = getEnv("KERNELEYE_INTERFACE", "eth0")
 	
+	// NEW: Auto-blocking env vars
+	if os.Getenv("KERNELEYE_AUTO_BLOCK") == "true" {
+		cfg.AutoBlockConfig = remediation.DefaultAutoBlockerConfig()
+		cfg.AutoBlockConfig.Enabled = true
+		// Parse other env vars...
+	}
+	
 	return cfg
 }
